import 'package:flutter/material.dart';
import 'package:flutter_learn/HomeView/HomeView.dart';

class appHome extends StatelessWidget {
  const appHome({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return HomeWidget();
  }
}
